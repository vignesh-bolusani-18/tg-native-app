// D:\TG_REACT_NATIVE_MOBILE_APP\components\agent\VibeGradientPage.js
import VibeGradientLayout from "./VibeGradientLayout";

const VibeGradientPage = () => {
  console.log("VibeGradientPage Rendered");
  
  // In Expo Router, the routing logic (/:conversationId) is handled by the file system (app/chat/[id].tsx).
  // This component simply wraps the main layout.
  return <VibeGradientLayout />;
};

export default VibeGradientPage;